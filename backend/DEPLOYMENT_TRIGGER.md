# Deployment Trigger
# This file forces Render to redeploy when changed

DEPLOYMENT_VERSION=2024-12-19-v3
LAST_UPDATE=Excel routes added via auth backup
COMMIT_HASH=ae191c3

# Routes that should be available after deployment:
# GET /api/auth/excel-test
# GET /api/auth/excel-template  
# POST /api/auth/excel-upload

# If this deployment version doesn't match what's live,
# the deployment hasn't completed yet.